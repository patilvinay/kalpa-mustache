const assert = require('assert');
const kalpaMustache = require('../index.js');

describe('kalpaMustache', () => {
  it('has a test', () => {
    assert(false, 'kalpaMustache should have a test');
  });
});
